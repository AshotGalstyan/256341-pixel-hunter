import AbstractView from '../../common/abstract-view.js';

export default class GameView extends AbstractView {
  get template() {
    return `
      <section>
        <p>Gnum a KASHY</p>
      </section>
      `;
  }
}
